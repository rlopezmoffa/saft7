<%= form_with(model: administracion) do |form| %>
  <% if administracion.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(administracion.errors.count, "error") %> prohibited this administracion from being saved:</h2>

      <ul>
        <% administracion.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :nombre, style: "display: block" %>
    <%= form.text_field :nombre %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
